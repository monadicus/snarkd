use crate::circuit::{
    traits::One,
    types::{Boolean, Scalar},
};

// impl One for Scalar {
//     fn one() -> Self {
//         Self::constant(Scalar::one())
//     }

//     fn is_one(&self) -> Boolean {
//         self.is_equal(&Self::one())
//     }
// }
